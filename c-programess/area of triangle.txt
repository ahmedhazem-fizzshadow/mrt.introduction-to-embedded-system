#define cout printf
#define cin scanf("%f"
#include <stdio.h>
//i was trying the define function
int main() {
  float hight,base;
    cout("pls enter the hight and the base of the triangle\n");
    cin,&hight);
    cin,&base);
    float area =0.5* hight * base;
    cout("the area of the triangle is : %f\n",area);
    
    
    
    
    

    return 0;
}